/**
 * Projects section — disabled until personal projects are ready.
 * Re-enable by uncommenting the import/usage in `src/App.tsx`
 * and adding your own project cards here.
 */
export default function Projects() {
  return null;
}
